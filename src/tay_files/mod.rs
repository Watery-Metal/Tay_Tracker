/// Tay-File Management for Reading and Writing Schedule Data

use std::{fs::OpenOptions};

pub fn read_schedule(file_path: &str) -> () {
    println!("read_schedule() was called. No implementation...");
}